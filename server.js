require('dotenv').config();
const express = require('express'), { spawn } = require('node:child_process'), fs = require('node:fs'), path = require('node:path'), crypto = require('node:crypto'), Database = require('better-sqlite3');
const root = __dirname, inputRoot = path.resolve(process.env.INPUT_ROOT || root), out = path.resolve(process.env.OUTPUT_DIR || path.join(root, 'output')), logs = path.resolve(process.env.LOG_DIR || path.join(root, 'logs')), dbPath = path.resolve(process.env.DB_PATH || path.join(root, 'jobs.db')), script = path.resolve(process.env.UPSCALE_SCRIPT || path.join(root, 'upscale_iris_720p.ps1'));
fs.mkdirSync(out, { recursive: true }); fs.mkdirSync(logs, { recursive: true });
const db = new Database(dbPath); db.exec('CREATE TABLE IF NOT EXISTS jobs(id TEXT PRIMARY KEY,input TEXT,output TEXT,status TEXT,created_at TEXT,started_at TEXT,finished_at TEXT,duration_ms INTEGER,exit_code INTEGER,log_file TEXT)'); let busy = false;
function worker() { if (busy) return; const j = db.prepare("SELECT * FROM jobs WHERE status='queued' ORDER BY created_at LIMIT 1").get(); if (!j) return; busy = true; const t = Date.now(), s = new Date().toISOString(); db.prepare('UPDATE jobs SET status=?,started_at=? WHERE id=?').run('running', s, j.id); const log = fs.createWriteStream(j.log_file, { flags: 'a' }); const p = spawn('powershell.exe', ['-NoProfile', '-ExecutionPolicy', 'Bypass', '-File', script, '-InputFile', j.input, '-OutputFile', j.output], { windowsHide: true }); p.stdout.pipe(log); p.stderr.pipe(log); p.on('close', code => { const f = new Date().toISOString(), d = Date.now() - t; log.end(`[${f}] exit=${code} duration_ms=${d}\n`); db.prepare('UPDATE jobs SET status=?,finished_at=?,duration_ms=?,exit_code=? WHERE id=?').run(code === 0 ? 'completed' : 'failed', f, d, code, j.id); busy = false; setImmediate(worker); }); }
const app = express(); app.use(express.json()); app.post('/jobs', (q, r) => { const input = path.resolve(q.body?.input || path.join(inputRoot, 'orginal.mp4')); if (!input.startsWith(inputRoot + path.sep) || !fs.existsSync(input)) return r.status(400).json({ error: 'invalid input' }); const id = crypto.randomUUID(), output = path.join(out, `${path.basename(input, path.extname(input))}_${id}_720p_iris.mp4`), log = path.join(logs, id + '.log'); db.prepare('INSERT INTO jobs VALUES(?,?,?,?,?,NULL,NULL,NULL,NULL,?)').run(id, input, output, 'queued', new Date().toISOString(), log); worker(); r.status(202).json({ id, status: 'queued' }); });
app.get('/jobs/:id', (q, r) => { const j = db.prepare('SELECT id,input,output,status,created_at,started_at,finished_at,duration_ms,exit_code FROM jobs WHERE id=?').get(q.params.id); if (!j) return r.sendStatus(404); if (j.status === 'completed') j.download = `/jobs/${j.id}/download`; r.json(j); }); app.get('/jobs/:id/download', (q, r) => { const j = db.prepare("SELECT * FROM jobs WHERE id=? AND status='completed'").get(q.params.id); if (!j || !fs.existsSync(j.output)) return r.sendStatus(404); r.download(j.output); }); app.listen(process.env.PORT || 3000, () => { console.log('Topaz service listening'); worker(); });
